services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - VECTOR_STORE_PATH=/app/data/vector_store
    restart: unless-stopped
